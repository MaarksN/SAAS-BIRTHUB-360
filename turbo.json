{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["DATABASE_URL", "SKIP_ENV_VALIDATION", "NODE_ENV", "NEXT_PUBLIC_*"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
