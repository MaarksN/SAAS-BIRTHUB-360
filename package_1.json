{
  "name": "ai-agents",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Building Python app...'",
    "dev": "uvicorn main:app --reload",
    "lint": "echo 'Linting Python app...'",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "devDependencies": {}
}
